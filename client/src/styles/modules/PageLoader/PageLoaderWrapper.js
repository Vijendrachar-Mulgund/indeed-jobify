import styled from "styled-components";

const PageLoaderWrapper = styled.div`
  height: inherit;

  display: flex;
  justify-content: center;
  align-items: center;

  font-size: 1.5rem;
  font-weight: 500;
`;

export { PageLoaderWrapper };
